let numeros = [1, 3, 3, 4, 5, 6, 7, 8];

for (let i = 0; i < numeros.length; i++) {
  //console.log(numeros[i] * 2);

  if (numeros[i] % 2 == 0) {
     console.log("es par");
  } else {
     console.log("es impar");
  }
}
